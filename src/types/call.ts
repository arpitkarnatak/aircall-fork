export type ICall = {
    id: string,
    call_type: string,
    created_at: string,
    direction: string,
    duration: number,
    from: number,
    to: number,
    via: number,
    is_archived: boolean,
}