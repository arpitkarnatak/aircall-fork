import { Link } from "react-router-dom"
import styled from "styled-components"

export const CallDivStyleMain = styled(Link)`
    all: unset;
    display: block;
    padding: 8px 16px;
    border-radius: 8px;
    text-align: left;
    background: aliceblue;
`